#include "comunes.h"

int posicion = 0; // no se para que carajo almacena el retorno de actualizar_pantalla

Personaje personaje;

extern void personaje0_muniequitoEstatico(void);
extern void movimiento_caer(void);
extern void camara_actualizar_pantalla(void);
